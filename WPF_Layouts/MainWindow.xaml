<Window x:Class="WPF_Layouts.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPF_Layouts"
        mc:Ignorable="d"
        Title="MainWindow" Height="170" Width="600" MinHeight="100" MinWidth="200">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Button Grid.Row="0" Grid.Column="0" Content="StackPanel" Margin="0,10,0,10" x:Name="btnStackPanel"></Button>
        <Button Grid.Row="1" Grid.Column="0" Content="Dock" Margin="0,0,0,10" x:Name="btnDock"></Button>
        <Button Grid.Row="2" Grid.Column="0" Content="Grid" Margin="0,0,0,10" x:Name="btnGrid"></Button>
        <Button Grid.Row="3" Grid.Column="0" Content="Grid Splitter" Margin="0,0,0,10" x:Name="btnGridSplitter"></Button>
    </Grid>
</Window>
